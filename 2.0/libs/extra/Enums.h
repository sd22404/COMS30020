#pragma once

enum RenderMode {
    WIREFRAME,
    RASTERISED,
    RAYTRACED
};

enum LightingMode {
    FLAT,
    GOURAUD,
    PHONG
};

enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT,
    FORWARD,
    BACKWARD
};